// LICENSE : MIT
"use strict";

var _sentenceSplitter = require("sentence-splitter");

var _textlintUtilToString = require("textlint-util-to-string");

var _textlintUtilToString2 = _interopRequireDefault(_textlintUtilToString);

var _textlintRuleHelper = require("textlint-rule-helper");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var defaultOptions = {
    max: 100
};
module.exports = function (context) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

    var maxLength = options.max || defaultOptions.max;
    var helper = new _textlintRuleHelper.RuleHelper(context);
    var Syntax = context.Syntax,
        RuleError = context.RuleError,
        report = context.report;
    // toPlainText

    return _defineProperty({}, Syntax.Paragraph, function (node) {
        if (helper.isChildNode(node, [Syntax.BlockQuote])) {
            return;
        }
        // If a single Link node in the paragraph node, should be ignore the link length
        var isChildrenSingleLinkNode = node.children.length === 1 && node.children[0].type === Syntax.Link;
        if (isChildrenSingleLinkNode) {
            return;
        }
        var source = new _textlintUtilToString2.default(node);
        var text = source.toString();
        // empty break line == split sentence
        var sentences = (0, _sentenceSplitter.split)(text, {
            newLineCharacters: "\n\n"
        });
        sentences.forEach(function (sentence) {
            // TODO: should trim()?
            var sentenceText = sentence.value;
            // larger than > 100
            if (sentenceText.length > maxLength) {
                var currentLine = node.loc.start.line;
                var start = source.originalPositionFromPosition(sentence.loc.start);
                var startLine = start.line - 1 + currentLine;
                var index = source.originalIndexFromPosition(sentence.loc.start);
                report(node, new RuleError("Line " + startLine + " exceeds the maximum line length of " + maxLength + ".", {
                    index: index
                }));
            }
        });
    });
};
//# sourceMappingURL=sentence-length.js.map